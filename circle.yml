machine:
  node:
    version: 8

test:
  override:
    - $(npm bin)/commit-message-install --else "npm i cypress"
    - $(npm bin)/cypress verify
    - node index.js
    - node async.js
    - npm test
